<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QuranJourney.Pages.Courses.Beginner.Beginner_Correct_Char"
             Title="Beginner_Correct_Char"
             Background="White">
    <Grid RowDefinitions="Auto,*,Auto" Padding="0">

        <!-- 🔹 HEADER + Progress Bar -->
        <VerticalStackLayout Grid.Row="0" Padding="20,20,20,0" Spacing="12">
            <!-- Top Row: Back Button + Progress Bar -->
            <Grid ColumnDefinitions="Auto,*" HeightRequest="50">
                <!-- Back Button -->
                <Image x:Name="xIcon"
                        Source="x.png"
                        HeightRequest="18"
                        WidthRequest="28"
                        VerticalOptions="Center"
                        HorizontalOptions="Start">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="CancleIcon_Tapped" />
                    </Image.GestureRecognizers>
                </Image>

                <!-- Progress Bar -->
                <Grid Grid.Column="1" Margin="15,0,0,0" HeightRequest="14"
                       HorizontalOptions="FillAndExpand" VerticalOptions="Center">
                    <!-- Gray background -->
                    <BoxView BackgroundColor="#E0E0E0"
                              CornerRadius="7"
                              HeightRequest="14"
                              HorizontalOptions="FillAndExpand"
                              VerticalOptions="Center" />
                    <!-- Green fill -->
                    <BoxView x:Name="ProgressFill"
                              BackgroundColor="#58CC02"
                              CornerRadius="7"
                              HeightRequest="14"
                              WidthRequest="0"
                              HorizontalOptions="Start"
                              VerticalOptions="Center" />
                </Grid>
            </Grid>

            <!-- Question Label -->
            <Label x:Name="QuestionsLabel"
                     Text="Select the correct characters for 'ghaa'"
                    FontSize="20"
                    FontAttributes="Bold"
                    TextColor="Black"
                    Margin="0,0,0,0"/>
        </VerticalStackLayout>

        <!-- 🔹 Matching Pairs Grid -->
        <!-- Matching Pairs Grid -->
        <Grid Grid.Row="1"
      RowDefinitions="Auto,Auto,Auto,Auto,Auto"
      ColumnDefinitions="*,*"
      Padding="20"
      RowSpacing="15"
      ColumnSpacing="20"
      HorizontalOptions="Center"
      VerticalOptions="Center">
            <!-- Row 1 -->
            <Button Grid.Row="0" Grid.Column="0" HeightRequest="160" Text="غ"
            BackgroundColor="White"
            FontAttributes="Bold"
            BorderColor="#D3D3D3"
            BorderWidth="2"
            FontSize="40"
            WidthRequest="120"
            TextColor="Black"
            CornerRadius="10"/>
            <Button Grid.Row="0" Grid.Column="1" HeightRequest="160" Text="gh"
            BackgroundColor="White"
            WidthRequest="120"
            FontSize="40"
            FontAttributes="Bold"
            BorderColor="#D3D3D3"
            Clicked="CheckBtn" 
            BorderWidth="2"
            TextColor="Black"
            CornerRadius="10"/>

            <!-- Row 2 -->
            <Button Grid.Row="1" Grid.Column="0" HeightRequest="160" Text="غا"
            BackgroundColor="White"
            FontAttributes="Bold"
            BorderColor="#D3D3D3"
            BorderWidth="2"
            FontSize="40"
            WidthRequest="120"
            TextColor="Black"
            CornerRadius="10"/>
            <Button Grid.Row="1" Grid.Column="1" HeightRequest="160" Text="ghaa"
            BackgroundColor="White"
            FontAttributes="Bold"
            WidthRequest="120"
            BorderColor="#D3D3D3"
            FontSize="40"
            Clicked="CheckBtn"
            BorderWidth="2"
            TextColor="Black"
            CornerRadius="10"/>
        </Grid>

        <!-- 🔹 Bottom CHECK BUTTON -->
        <Button x:Name="CheckButton"
                 Grid.Row="2"
                 Text="CHECK"
                 FontSize="18"
                 FontAttributes="Bold"
                 BackgroundColor="#E0E0E0"
                 TextColor="White"
                 CornerRadius="14"
                Clicked="CheckBtn"
                 HeightRequest="55"
                 HorizontalOptions="FillAndExpand"
                 Margin="20,0,20,20" />
    </Grid>
</ContentPage>